#include <stdio.h>
/**
 * main - Displays "Programming is like a building a multilingual puzzle
 *
 * Return: returns the value 0 if exited properly, otherwise non-zero
 */
int main(void)
{
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	puts(Mystr);

	return (0); /*can be ommited*/
}
